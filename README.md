# Simple project JS starter :rocket:

## Install dependencies: `npm install`

## Working in development mode:
Run `webpack serve` in a terminal

## Prepare files for production:
Run `webpack --mode production`
